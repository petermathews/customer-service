# Core — rules + classic ML + image classification + the notebook
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7
notebook>=7.0

# GenAI + agent paths (also need ANTHROPIC_API_KEY)
anthropic>=0.40
langgraph>=0.2
langchain-anthropic>=0.2

# Optional — run the same Claude code on a cloud provider instead of the API:
#   pip install "anthropic[bedrock]"   # AWS  (AnthropicBedrock)
#   pip install "anthropic[vertex]"    # GCP  (AnthropicVertex)
